Filter tools
| Change date period | View all sections/one section picker |

|Students                          |
  | Sites visited  |  Time on site |
    [expand site]
    | Pages visited | Time on page | 

Nb. Time on site/page is for what time frame by default? (7 days?)

teacher.students.each do |student|
  student.full_name
    student.each_site_visited do |site|
      site.display_name | site.time_spent
        [open/close icon/js]
          site.each_page_visited do |page|
            page.display_name | page.time_spent
<%=%>
